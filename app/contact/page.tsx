import type { Metadata } from 'next';
import Link from 'next/link';
export const metadata: Metadata = { title: 'Contact | DetailFlow', description: 'Contact information for DetailFlow support and business inquiries.' };
export default function Contact(){const email=process.env.NEXT_PUBLIC_CONTACT_EMAIL||'';const address=process.env.NEXT_PUBLIC_CONTACT_ADDRESS||'';return <main className="shell section"><Link href="/" className="brand"><span className="brand-mark">DF</span> detailflow</Link><div className="kicker" style={{marginTop:60}}><span/> contact</div><h1>Contact DetailFlow.</h1><p className="section-intro">For support, billing, privacy questions, or business inquiries, use the contact details below.</p><div className="empty-state" style={{maxWidth:760,marginTop:30}}><h2>Support contact</h2>{email?<p><a href={`mailto:${email}`}>{email}</a></p>:<p className="field-help">The service operator should configure NEXT_PUBLIC_CONTACT_EMAIL before launch.</p>}{address?<><h3 style={{marginTop:24}}>Business address</h3><p style={{whiteSpace:'pre-line'}}>{address}</p></>:<p className="field-help">The service operator should configure NEXT_PUBLIC_CONTACT_ADDRESS before launch if a public business address is required.</p>}</div></main>}
