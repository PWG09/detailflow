import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Update password | DetailFlow', robots: { index: false, follow: false } };
export default function UpdatePasswordLayout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }
