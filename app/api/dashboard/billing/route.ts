import { NextResponse } from 'next/server';
import { createSupabaseServerClient } from '@/lib/supabase/server';
import { getAppUrl, getStripe } from '@/lib/stripe';
export const runtime='nodejs';
async function ctx(){const supabase=await createSupabaseServerClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return null;const {data:m}=await supabase.from('business_members').select('business_id,role').eq('user_id',user.id).limit(1).maybeSingle();return m?{supabase,user,m}:null;}
export async function GET(){const c=await ctx();if(!c)return NextResponse.json({error:'Not authenticated.'},{status:401});const {data:b}=await c.supabase.from('businesses').select('id,name,plan,stripe_customer_id,subscription_status').eq('id',c.m.business_id).single();return NextResponse.json(b);}
export async function POST(){const c=await ctx();if(!c)return NextResponse.json({error:'Not authenticated.'},{status:401});if(c.m.role!=='owner')return NextResponse.json({error:'Only the owner can manage billing.'},{status:403});const price=process.env.STRIPE_PRO_PRICE_ID;if(!price)return NextResponse.json({error:'STRIPE_PRO_PRICE_ID is not configured.'},{status:503});const {data:b}=await c.supabase.from('businesses').select('id,name,plan,stripe_customer_id').eq('id',c.m.business_id).single();if(b?.plan==='pro')return NextResponse.json({error:'This workspace is already on Pro.'},{status:409});const stripe=getStripe();const session=await stripe.checkout.sessions.create({mode:'subscription',line_items:[{price,quantity:1}],customer:b?.stripe_customer_id||undefined,customer_email:b?.stripe_customer_id?undefined:c.user.email||undefined,metadata:{businessId:c.m.business_id},success_url:`${getAppUrl()}/dashboard/settings?billing=success`,cancel_url:`${getAppUrl()}/dashboard/settings?billing=cancelled`});return NextResponse.json({url:session.url});}
export async function PATCH(){const c=await ctx();if(!c)return NextResponse.json({error:'Not authenticated.'},{status:401});if(c.m.role!=='owner')return NextResponse.json({error:'Only the owner can manage billing.'},{status:403});const {data:b}=await c.supabase.from('businesses').select('stripe_customer_id,stripe_subscription_id').eq('id',c.m.business_id).single();if(!b?.stripe_customer_id)return NextResponse.json({error:'No Stripe customer exists yet.'},{status:400});const session=await getStripe().billingPortal.sessions.create({customer:b.stripe_customer_id,return_url:`${getAppUrl()}/dashboard/settings`});return NextResponse.json({url:session.url});}
