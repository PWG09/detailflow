import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Request a quote | DetailFlow', description: 'Submit vehicle details and photos to request a detailing quote.', openGraph: { title: 'Request a quote | DetailFlow', description: 'Submit vehicle details and photos to request a detailing quote.', type: 'website' } };
export default function BusinessQuoteLayout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }
