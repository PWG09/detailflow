'use client';
import { useState } from 'react';
import { ArrowLeft, ArrowRight, Check, Upload } from 'lucide-react';

const services = [['Interior detail', '$150–220'], ['Exterior detail', '$120–180'], ['Full detail', '$250–350'], ['Ceramic coating', 'from $800']];
const vehicles = ['Sedan', 'SUV', 'Truck', 'Coupe', 'Van', 'Motorcycle'];

export default function QuotePage() {
  const [step, setStep] = useState(1); const [service, setService] = useState('Full detail'); const [submitted, setSubmitted] = useState(false);
  const next = () => setStep((current) => Math.min(3, current + 1));
  if (submitted) return <main className="quote-page"><div className="shell quote-header"><a className="brand" href="/"><span className="brand-mark">DF</span> northline detailing</a></div><div className="shell" style={{maxWidth:720,paddingTop:110,textAlign:'center'}}><div className="brand-mark" style={{margin:'0 auto',transform:'none'}}><Check size={18}/></div><h1 style={{fontSize:55,margin:'25px auto 18px'}}>Request received.</h1><p className="form-intro" style={{maxWidth:410,margin:'auto'}}>Thanks, Jordan. Northline Detailing will review your vehicle details and get back to you shortly.</p><a href="/" className="button-primary" style={{marginTop:30}}>Back to DetailFlow <ArrowRight size={15}/></a></div></main>;
  return <main className="quote-page"><div className="shell quote-header"><a className="brand" href="/"><span className="brand-mark">DF</span> northline detailing</a></div><div className="shell quote-layout"><aside className="quote-aside"><div className="kicker"><span/> free estimate</div><h1>What does your vehicle need?</h1><p>Tell us a little about the vehicle and we’ll put together a clear starting range. No pressure, no guesswork.</p><div className="quote-aside-foot">Typical response time<br/><strong style={{color:'var(--ink)'}}>under 2 hours</strong></div></aside><section className="quote-form-wrap"><div className="progress">{[1,2,3].map((item) => <i className={item <= step ? 'active' : ''} key={item}/>)}</div>{step === 1 && <><h2>Start with the basics.</h2><p className="form-intro">First, tell us what you’re bringing in.</p><div className="form-grid"><div className="field"><label htmlFor="year">Year</label><input id="year" placeholder="2021" /></div><div className="field"><label htmlFor="make">Make and model</label><input id="make" placeholder="Porsche Macan" /></div><div className="field full"><label>Vehicle type</label><div className="service-options">{vehicles.map((item) => <button type="button" className={'service-option '+(item === 'SUV' ? 'selected':'')} key={item}><strong>{item}</strong></button>)}</div></div></div><div className="form-actions"><span className="mono" style={{fontSize:11,color:'var(--muted)'}}>1 of 3</span><button className="button-primary" onClick={next}>Continue <ArrowRight size={15}/></button></div></>}{step === 2 && <><h2>Choose a service.</h2><p className="form-intro">Select what you’re looking to have done.</p><div className="service-options">{services.map(([name, price]) => <button type="button" className={'service-option '+(service === name ? 'selected':'')} onClick={() => setService(name)} key={name}><strong>{name}</strong><small>{price}</small></button>)}</div><div className="field"><label htmlFor="condition">Anything we should know?</label><textarea id="condition" rows={4} placeholder="Pet hair, stains, paint concerns, or anything else..."></textarea></div><div className="form-actions"><button className="back-button" onClick={() => setStep(1)}><ArrowLeft size={14}/> Back</button><button className="button-primary" onClick={next}>Add your details <ArrowRight size={15}/></button></div></>}{step === 3 && <><h2>Where should we send your estimate?</h2><p className="form-intro">Your photos help us understand the job. They’re only shared with Northline Detailing.</p><div className="field"><label className="service-option" style={{display:'flex',alignItems:'center',gap:12}}><Upload size={20} color="var(--orange)"/><span><strong>Upload vehicle photos</strong><small>Up to 8 JPG or PNG images</small></span><input type="file" multiple accept="image/png,image/jpeg" style={{display:'none'}} /></label></div><div className="form-grid"><div className="field"><label htmlFor="first">First name</label><input id="first" placeholder="Jordan" /></div><div className="field"><label htmlFor="last">Last name</label><input id="last" placeholder="Lee" /></div><div className="field"><label htmlFor="email">Email</label><input id="email" type="email" placeholder="jordan@email.com" /></div><div className="field"><label htmlFor="phone">Phone</label><input id="phone" placeholder="(555) 014-8820" /></div></div><div className="estimate"><small>your current estimate</small><strong>{service === 'Ceramic coating' ? '$800+' : service === 'Full detail' ? '$250–350' : '$150–220'}</strong><p>Based on a {service.toLowerCase()} for an SUV. Final pricing is confirmed after review.</p></div><div className="form-actions"><button className="back-button" onClick={() => setStep(2)}><ArrowLeft size={14}/> Back</button><button className="button-primary" onClick={() => setSubmitted(true)}>Request my estimate <ArrowRight size={15}/></button></div></>}</section></div></main>;
}
