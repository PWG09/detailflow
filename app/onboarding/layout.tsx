import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Create your workspace | DetailFlow', description: 'Set up a DetailFlow workspace for your detailing business.', robots: { index: false, follow: false } };
export default function OnboardingLayout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }
