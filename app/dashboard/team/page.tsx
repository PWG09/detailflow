'use client';
import { FormEvent, useEffect, useState } from 'react';
import Link from 'next/link';
import { ArrowLeft, UserPlus } from 'lucide-react';
type Member={user_id:string;role:string;created_at:string};
export default function TeamPage(){const [members,setMembers]=useState<Member[]>([]);const [email,setEmail]=useState('');const [message,setMessage]=useState('');const [role,setRole]=useState('');useEffect(()=>{void fetch('/api/dashboard/team').then(r=>r.json()).then(x=>{setMembers(x.members||[]);setRole(x.currentRole||'')})},[]);async function submit(e:FormEvent){e.preventDefault();setMessage('');const r=await fetch('/api/dashboard/team',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const x=await r.json();setMessage(r.ok?'Invitation sent.':x.error||'Unable to invite.');if(r.ok){setEmail('');void fetch('/api/dashboard/team').then(r=>r.json()).then(x=>setMembers(x.members||[]));}}return <section className="dashboard-main"><Link href="/dashboard/settings" className="button-secondary"><ArrowLeft size={15}/> Back to settings</Link><div className="dash-header" style={{marginTop:34}}><div><span className="mono eyebrow">Workspace</span><h1>Team</h1><p>Invite staff without giving them billing or ownership controls.</p></div></div><div className="form-grid"><form className="empty-state" onSubmit={submit}><h3>Invite staff</h3><div className="field"><label>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><button className="button-primary" disabled={role!=='owner'}><UserPlus size={15}/> Send invite</button>{role!=='owner'&&<p className="auth-message">Only the workspace owner can invite staff.</p>}{message&&<p role="status" className="auth-message">{message}</p>}</form><div className="empty-state"><h3>Members</h3>{members.map(m=><div key={m.user_id} style={{display:'flex',justifyContent:'space-between',padding:'12px 0',borderBottom:'1px solid #e5e7eb'}}><span>{m.user_id}</span><strong>{m.role}</strong></div>)}</div></div></section>}
