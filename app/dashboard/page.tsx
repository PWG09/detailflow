import Link from 'next/link';
import { BarChart3, CarFront, ChevronRight, ClipboardList, Gauge, Settings, Users, Wrench } from 'lucide-react';

const nav = [['Overview', Gauge, '/dashboard'], ['Leads', ClipboardList, '/dashboard/leads'], ['Customers', Users, '/dashboard/customers'], ['Quotes', CarFront, '/dashboard/quotes'], ['Services', Wrench, '/dashboard/services'], ['Analytics', BarChart3, '/dashboard/analytics'], ['Settings', Settings, '/dashboard/settings']] as const;

export default function Dashboard() {
  return <main className="dashboard-page"><aside className="dashboard-sidebar"><Link className="brand" href="/"><span className="brand-mark">DF</span> detailflow</Link><div className="dashboard-nav">{nav.map(([label, Icon, href], index) => <Link className={index === 0 ? 'active' : ''} href={href} key={label}><Icon size={16} />{label}</Link>)}</div><div className="dashboard-user"><div className="avatar">JL</div><div><strong>Jordan Lee</strong><small>Northline Detailing</small></div></div></aside><section className="dashboard-main"><div className="dash-header"><div><span className="mono eyebrow">Tuesday, June 18</span><h1>Good morning, Jordan.</h1><p>Here’s what’s happening with your shop.</p></div><Link href="/quote" className="button-primary">Create public quote <ChevronRight size={15} /></Link></div><div className="dashboard-metrics"><div><span>Leads this month</span><strong>0</strong><small>Your first lead is one link away.</small></div><div><span>Quotes sent</span><strong>0</strong><small>Build trust before the inbox gets busy.</small></div><div><span>Conversion rate</span><strong>—</strong><small>Calculated after your first quote.</small></div></div><div className="empty-state"><div className="empty-icon"><ClipboardList size={22} /></div><h2>Your lead inbox is ready.</h2><p>Share your public quote link to start collecting vehicle details, photos, and qualified requests in one place.</p><Link className="button-primary" href="/quote">Create public quote <ChevronRight size={15} /></Link></div></section></main>;
}
