import { BarChart3, CarFront, ChevronRight, ClipboardList, Gauge, Settings, Users, Wrench } from 'lucide-react';

const nav = [['Overview', Gauge], ['Leads', ClipboardList], ['Customers', Users], ['Quotes', CarFront], ['Services', Wrench], ['Analytics', BarChart3], ['Settings', Settings]] as const;
export default function Dashboard() { return <main className="dashboard-page"><aside className="dashboard-sidebar"><a className="brand" href="/"><span className="brand-mark">DF</span> detailflow</a><div className="dashboard-nav">{nav.map(([label, Icon], index) => <a className={index === 0 ? 'active' : ''} href="#" key={label}><Icon size={16}/>{label}</a>)}</div><div className="dashboard-user"><div className="avatar">JL</div><div><strong>Jordan Lee</strong><small>Northline Detailing</small></div></div></aside><section className="dashboard-main"><div className="dash-header"><div><span className="mono eyebrow">Tuesday, June 18</span><h1>Good morning, Jordan.</h1><p>Here’s what’s happening with your shop.</p></div><a href="/quote/demo-detailing" className="button-primary">View public link <ChevronRight size={15}/></a></div><div className="dashboard-metrics"><div><span>Leads this month</span><strong>0</strong><small>Your first lead is one link away.</small></div><div><span>Quotes sent</span><strong>0</strong><small>Build trust before the inbox gets busy.</small></div><div><span>Conversion rate</span><strong>—</strong><small>Calculated after your first quote.</small></div></div><div className="empty-state"><div className="empty-icon"><ClipboardList size={22}/></div><h2>Your lead inbox is ready.</h2><p>Share your public quote link to start collecting vehicle details, photos, and qualified requests in one place.</p><a className="button-primary" href="/quote/demo-detailing">Open quote flow <ChevronRight size={15}/></a></div></section></main> }
